#include<stdio.h>

 void main(){
  float hight,base,area;
  printf ("Write the hight of tringle:");
  scanf("%f",&hight);
  printf ("\nWrite the base of tringle:");
  scanf("%f",&base);
  area=hight*base*0.5;
  printf("Area of the tringle:""%f",area);
 }
